doctype html
html.h-100(lang="ru")
    head
        title= Задачи
        meta(name="viewport" content="width=device-width, initial-scale=1")
        script.
          var gon = !{JSON.stringify(gon)};
    body.h-100
        #tasks
        script(src='/assets/tasks.bundle.js')
